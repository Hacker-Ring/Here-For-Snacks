package com.stride.stride.REPOSITORY;

import com.stride.stride.MODEL.REPO;
import org.springframework.data.jpa.repository.JpaRepository;

public interface REPOREPOSITORY extends JpaRepository<REPO, Long> { }

